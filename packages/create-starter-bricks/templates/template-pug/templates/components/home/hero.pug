mixin Hero()
    .container
        div(class="mt-12")
            img(src=images.logo, width='200')
        h1(class="text-4xl md:text-[120px] font-bold")=page.title
        p(class="mt-10") Create projects from ready-to-use frontend templates.
